import {StyleSheet} from 'react-native';

const globalStyles = () =>
  StyleSheet.create({
    backgroundImage: {
      width: '100%',
      height: '100%',
      alignItems: 'center',
    },
  });

export default globalStyles;
